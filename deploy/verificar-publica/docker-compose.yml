services:
  verificar-nginx:
    image: nginx:1.25-alpine
    container_name: verificar-nginx
    restart: unless-stopped
    network_mode: host
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ./html:/usr/share/nginx/html:ro
